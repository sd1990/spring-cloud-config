spring:
  application:
    name: config-client
  cloud:
    services:
      registrationMethod: route
    config:
      name: props
      enabled: true
      uri:  http://localhost:8001

server:
  port: 8002


