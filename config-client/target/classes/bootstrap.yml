spring:
  application:
    name: config-client
  profiles:
    active: dev
  cloud:
    services:
      registrationMethod: route
    config:
      failFast: true
      name: prop
      enabled: true
      uri:  http://localhost:8001

server:
  port: 8002


